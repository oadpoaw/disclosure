import { default as disable } from './disable';
import { default as enable } from './enable';
import { default as eval } from './eval';
import { default as help } from './help';
import { default as ping } from './ping';

export default { disable, enable, eval, help, ping };

